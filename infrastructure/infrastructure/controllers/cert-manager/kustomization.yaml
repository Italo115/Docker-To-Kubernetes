apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization
# cert-manager namespace is owned by secrets-infrastructure (declared there so
# the cloudflare-api Secret applies before this HelmRelease reconciles).
resources:
  - release.yaml
